# ./src/repo/loaders/query-result

This package contains interfaces for wrappers for table data that is manually extracted from raw SQL operations. Please note, FK relations are represented by the raw table columns.