describe("Entity load service", () => {
  /**
   * Validate that data required for trace operation is properly loaded
   * and relations are established.
   *
   */
  test("Load data structure", () => {});
});
